[package]
name = "poketto-app"
version.workspace = true
edition.workspace = true

[dependencies]
poketto-core = { path = "../poketto-core" }
slint.workspace = true

[build-dependencies]
slint-build.workspace = true
